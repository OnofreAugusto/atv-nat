#include<stdio.h>
int main ()
{int i, idade, quanthomens, quantmuie, mediageral, mediahomens, muieadulta, soma;
    int cont_m = 0, cont_f = 0;
    char sexo[9];
    while (i<5)
    {
     printf("Insira a idade de uma pessoa: ");
     scanf(idade);   

     printf("E qual o seu sexo? ");
     scanf(" %c", sexo);

        if (sexo == "Masculino" || sexo == "masculino")
        {
            cont_m++;
        }
        if (sexo == "Feminino" || sexo == "feminino")
        {
            cont_f++;
        }
        mediageral = idade / 10;
        if (condition)
        {
            /* code */
        }
        

    }
    
     







    return 0;
}